{"type":"result","subtype":"success","is_error":false,"duration_ms":360375,"duration_api_ms":360057,"num_turns":42,"result":"Empty placeholder. Good — writing the full review now.\n\n---\n\n# Code Review: Degree Tracker\n\n**Reviewer:** Albert, QA Manager\n**Date:** 2026-04-25\n**Verdict:** **PASS WITH FIXES** — Core architecture is solid, but two issues will cause real functional failures in normal use.\n\n---\n\n## 1. Secret Scan\n\n🟢 **Clean.** No API keys, tokens, private keys, or hardcoded credentials in any source file. The only \"sensitive\" data is `studentId` in `ProgramInfo` (CU's 9-digit ID), which is expected given this is a personal audit import tool running on localhost. Acceptable.\n\n---\n\n## 2. Issues by Severity\n\n---\n\n### 🔴 Critical\n\n**[C1] `getSemesterStatus` hardcodes Spring 2026 as current semester**\n`lib/parser.ts:851–864`\n\n```typescript\nif (yearNum < 2026) return \"completed\";\nif (yearNum > 2026) return \"planned\";\nif (season === \"SP\") return \"in_progress\"; // Spring 2026 is current\n```\n\nThis is the most consequential bug in the codebase. When you upload a new audit in Fall 2026 or later, every semester will be mis-classified. FA26 courses will be marked \"planned\" when they're in-progress, and prior completed semesters from 2025 will still be fine — but this is a time bomb that breaks the tool's primary function the moment you leave SP26.\n\n**Fix:** Derive the current semester from the `PREPARED:` date already parsed in `extractProgramInfo`. The date is captured as `preparedDate`. Parse it, extract the term (April → SP, September → FA, June/July → SU), and use that to classify relative semesters instead of hardcoding `2026`.\n\n---\n\n**[C2] Stub courses never created for `SELECT FROM` elective pools**\n`lib/parser.ts:619–623`\n\n```typescript\n// Build Course objects from table entries\nfor (const rc of sg.courses) {    // ← only course TABLE rows get objects\n  const course = rawCourseToCourse(rc, warnings);\n```\n\nCourses in `sg.selectFrom` (the elective pool from `SELECT FROM:` lines) are correctly added to `coursePool` IDs, but no `Course` objects are ever created for them. Every unstarted elective option will show as **\"not in system\"** in:\n- The elective picker (requirements page)\n- The course catalog (can't search/view them)\n- The semester planner (can't drag them into semesters)\n- The prereq tree viewer (no nodes to render)\n\nGiven that the entire point of the elective picker and major UD elective planning is to choose from the pool, this essentially breaks the core `pick_n` workflow until the prereq scraper is run. The audit PDF has course numbers in those `SELECT FROM` lines — the parser should create stub `Course` objects (`status: \"not_started\"`, empty name, 0 credits) for any `selectFrom` ID that doesn't already have a full entry.\n\n---\n\n### 🟡 Warnings\n\n**[W1] No `.catch()` on any `Promise.all` fetch chain**\n`app/planner/page.tsx:363`, `app/gpa/page.tsx:43`, `app/courses/page.tsx:249`, `app/requirements/page.tsx:329`\n\nPattern repeated in all four client pages:\n```typescript\nPromise.all([\n  fetch(\"/api/courses\").then((r) => r.json()),\n  fetch(\"/api/semesters\").then((r) => r.json()),\n]).then(([c, s]) => {\n  // ...\n  setLoading(false);\n});  // no .catch()\n```\n\nIf the API throws (JSON parse error on empty data, fetch network error, etc.), `loading` stays `true` forever. The page is stuck on \"Loading…\" with no error message and no way out. This will happen the first time a user opens the app with corrupted JSON data.\n\n**Fix:** Add `.catch((err) => { setError(String(err)); setLoading(false); })` to each chain.\n\n---\n\n**[W2] `gradeToPoints` duplicated with divergent return values**\n`lib/parser.ts:808` vs `lib/prereqs.ts:33`\n\nParser version: returns `0.0` for unknown grades.\nPrereqs version: returns `-1` for unknown grades (which is then filtered out in GPA calculations).\n\nThe parser sets `gradePoints` on completed courses using its `0.0`-returning version. A course with an unknown grade (e.g., pass/fail \"CR\" or audit \"AU\") gets `gradePoints: 0`. The GPA display in the course catalog then shows 0.0 quality points instead of no value. Not catastrophic but misleading.\n\n---\n\n**[W3] Missing `key` prop on React Fragment in course catalog**\n`app/courses/page.tsx:371`\n\n```tsx\n{filtered.map((course) => {\n  return (\n    <>              {/* ← Fragment needs key={course.id} */}\n      <tr key={course.id} ...>\n      {expanded && <tr key={`${course.id}-exp`}>}\n    </>\n  );\n})}\n```\n\nThe `key` is on the `<tr>`, not the wrapping `<>`. React will warn in dev and may produce incorrect reconciliation in React 19's more strict mode. Change `<>` to `<React.Fragment key={course.id}>`.\n\n---\n\n**[W4] Hardcoded `120` credit hours in dashboard**\n`app/page.tsx:107, 110, 115`\n\n```tsx\nsub=\"of 120 required\"\nvalue={`${Math.round((totalEarned / 120) * 100)}%`}\nvalue={Math.max(0, 120 - totalEarned - totalIP).toString()}\n```\n\nThe spec explicitly states: **\"The app does NOT hardcode requirement structures.\"** The 120-hour requirement should come from a `minimum_hours` requirement group or `ProgramInfo`. This is currently just a cosmetic issue but will produce wrong numbers if the total hours requirement differs.\n\n---\n\n**[W5] `plannedCredits` computed but never used — spec's Alerts panel is absent**\n`app/page.tsx:81–84`\n\n```typescript\nconst plannedCredits = semesters\n  .filter((s) => s.status === \"planned\")\n  .flatMap((s) => s.courses.map(...))\n  .filter(Boolean);\n```\n\nDead variable. The spec calls for an **Alerts panel** in section 3.1 (\"prereq conflicts, missing requirements, credit load warnings\"). The variable looks like an abandoned start of that feature. The dashboard currently has no alerts whatsoever.\n\n---\n\n**[W6] Dashboard `currentCourses` can double-count**\n`app/page.tsx:72–74`\n\n```typescript\nconst currentCourses = courses.filter(\n  (c) => c.status === \"in_progress\" || (currentSem && currentSem.courses.includes(c.id))\n);\n```\n\nA course that is `in_progress` AND has its ID in `currentSem.courses` passes both conditions. `Array.filter` doesn't deduplicate, so it appears twice in the current semester card.\n\n---\n\n**[W7] Grade minimum tracking not implemented**\n`lib/types.ts`\n\nThe spec (Section 8, Resolved Question 3): *\"Flag grades below the requirement group minimum (e.g., C- for CS Minor).\"* `RequirementGroup` has no `minGrade` field. The parser doesn't extract it, the UI doesn't display it, and the planner doesn't warn about it. Complete gap.\n\n---\n\n**[W8] `@dnd-kit/sortable` in dependencies, never imported**\n`package.json:13`\n\nInstalled but zero imports anywhere in the codebase. Unused dependency.\n\n---\n\n### 🟢 Minor\n\n**[M1] `formatCourseId` missing `/g` flag**\n`app/planner/page.tsx:62`, `app/courses/page.tsx:31`, `app/requirements/page.tsx:72`\n\n```typescript\nreturn id.replace(\"-\", \" \");  // only replaces first hyphen\n```\n\nCurrent IDs are like `STAT-3100` (single hyphen), so this works. But it's misleading and would break if IDs ever have multiple hyphens. Use `/g` or `.replaceAll`.\n\n---\n\n**[M2] `calcProgressClient` duplicated from `lib/data.ts:calcProgress`**\n`app/requirements/page.tsx:10–46`\n\nClient-server boundary forced this, but the two implementations can drift. `calcProgress` should be moved to `lib/prereqs.ts` (which is already client-safe — no `fs` imports) so both server and client share one implementation. Right now it lives in `lib/data.ts` alongside the `fs` I/O code, which is why it can't be imported client-side.\n\n---\n\n**[M3] Optimistic update revert uses stale closure**\n`app/planner/page.tsx:443–446`\n\n```typescript\n} catch {\n  setAssignments(assignments);  // stale closure ref\n}\n```\n\nIf two moves happen in quick succession, the catch block from move #2 could revert the assignments state to what it was before move #1, losing move #1's optimistic state. In practice, single-user localhost, this never happens. Still wrong.\n\n---\n\n**[M4] Semester status derivation from 2-digit years**\n`lib/parser.ts:855`\n\n```typescript\nconst yearNum = parseInt(\"20\" + shortTerm.slice(2), 10);\n```\n\nAlways prepends \"20\". A course from FA99 becomes year 2099. You don't have pre-2000 courses so this is practically fine — just document the assumption.\n\n---\n\n**[M5] `normalizePrereqData` does no type validation on array inputs**\n`app/api/prereqs/import/route.ts:93`\n\n```typescript\nif (Array.isArray(raw)) return raw as PrereqEntry[];\n```\n\nCast without validation. Malformed JSON from the scraper (wrong field names, null courseId, etc.) will silently corrupt courses.json. Add a basic shape check before the cast.\n\n---\n\n**[M6] `SELECT FROM` continuation regex too permissive**\n`lib/parser.ts:511`\n\n```typescript\nif (/^\\s+[A-Z0-9,\\s]+$/.test(line) && !RE.NEEDS.test(line)) {\n```\n\nThis would match lines like `A GRADE OF C OR BETTER REQUIRED` as course IDs. `expandCourseIds` would produce no valid IDs from that text and continue, but it's a fragile assumption. Worth adding a minimum token length check or validating that extracted tokens look like actual course IDs.\n\n---\n\n**[M7] Missing Settings page**\nSpec section 6 mentions \"Settings page: manage semesters, import audits, edit courses manually.\" No settings page exists, and it's not in the sidebar nav. Not blocking, but incomplete against spec.\n\n---\n\n**[M8] Target grade solver is cumulative-only**\n`app/gpa/page.tsx:100–107`\n\nThe spec says: *\"What grade do I need in X to get a 3.0 **major GPA**?\"* The solver only calculates against cumulative GPA. No major GPA target mode.\n\n---\n\n## 3. Parser-Specific Assessment\n\nThe parser architecture (4-phase: header → sections → requirement groups → history) is sensible. The regex design handles the messy CU audit format reasonably well. The `SELECT FROM` expansion, `MATCHED AS` annotation tracking, and course deduplication logic are all correct.\n\n**What it gets right:**\n- Correctly identifies `complete_all` (course table rows), `pick_one` (multi-course SELECT FROM), and `pick_n` (description-keyed)\n- `parseGrade` correctly handles `***`, `W`, `HS`, `NR`, `I`, grade modifiers (`>N`, `>R`, etc.)\n- `normalizeTermCode` / `fullTermToShort` conversion is consistent\n- History section correctly extracts semester data and cross-references to sections\n\n**What it gets wrong:**\n- [C1] hardcoded current semester\n- [C2] no stub courses for unstarted elective options\n- `determineGroupType` will misclassify any multi-course `SELECT FROM` group that requires completing all options (defaults to `pick_one` unless description says \"complete all\"). This could affect some requirement groups.\n- The 60-line header scan limit (`Math.min(lines.length, 60)`) will silently fail to extract program info if the header is longer than 60 lines (unlikely but possible with verbose audit layouts).\n\n---\n\n## 4. GPA Math Correctness\n\nVerified correct. `calcGPA` and `solveTargetGrade` in `lib/prereqs.ts` implement standard quality points GPA correctly. `HS` grades are excluded. The what-if mode correctly unions completed grades with hypothetical grades without double-counting.\n\n---\n\n## 5. Prereq Validation Correctness\n\n`validateDrop` in `lib/prereqs.ts` correctly:\n- Separates \"before target semester\" (prereq set) from \"at-or-before\" (coreq set)\n- Treats `completed` status courses as always available regardless of semester assignment\n- Handles `and`/`or` rules correctly\n\n`getCascadeWarnings` correctly identifies downstream courses broken when a course moves later.\n\nOne note: the cascade check only fires when moving to a **later** semester (`toIdx > fromIdx`). Moving to an **earlier** semester still runs `validateDrop` on the moved course's own prereqs, but doesn't check if that course is a prerequisite for courses in the intermediate semesters that now have their prereq moved earlier than needed. This edge case (moving a course EARLIER could create a prereq conflict in a course between the two positions) is not handled. Low priority but worth knowing.\n\n---\n\n## 6. Architecture Notes\n\n**JSON file storage:** Fine for this scale. The read-modify-write pattern is not atomic (race condition possible), but single-user localhost makes this a non-issue in practice.\n\n**Server vs client component split:** Correct. Dashboard is `force-dynamic` server component reading JSON directly. All interactive pages are `\"use client\"`. The server-only `fs` code stays in `lib/data.ts` and is never imported by client components.\n\n**PDF parse workaround:** The `require(\"pdf-parse/lib/pdf-parse.js\")` direct import is the correct workaround for pdf-parse's well-known Next.js initialization bug. Not a hack — it's the documented fix.\n\n**No `@dnd-kit/sortable` usage:** The planner correctly uses only `@dnd-kit/core` (drag + drop between containers), not sortable (reordering within a list). The installed-but-unused package is leftover.\n\n---\n\n## Summary\n\n| # | Severity | Item | File |\n|---|----------|------|------|\n| C1 | 🔴 Critical | Hardcoded SP26 as current semester | `lib/parser.ts:851` |\n| C2 | 🔴 Critical | No stub courses for elective pools | `lib/parser.ts:619` |\n| W1 | 🟡 Warning | No fetch error handling (all client pages) | `app/*/page.tsx` |\n| W2 | 🟡 Warning | Duplicate `gradeToPoints` with divergent returns | `lib/parser.ts:808` |\n| W3 | 🟡 Warning | Fragment missing `key` prop | `app/courses/page.tsx:371` |\n| W4 | 🟡 Warning | Hardcoded 120 credit hours | `app/page.tsx:107` |\n| W5 | 🟡 Warning | Dead `plannedCredits` / alerts panel missing | `app/page.tsx:81` |\n| W6 | 🟡 Warning | `currentCourses` double-count | `app/page.tsx:72` |\n| W7 | 🟡 Warning | Grade minimum tracking unimplemented | `lib/types.ts` |\n| W8 | 🟡 Warning | Unused `@dnd-kit/sortable` dependency | `package.json:13` |\n| M1 | 🟢 Minor | `replace(\"-\", \" \")` missing `/g` flag | multiple |\n| M2 | 🟢 Minor | `calcProgressClient` duplication | `app/requirements/page.tsx` |\n| M3 | 🟢 Minor | Stale closure in optimistic revert | `app/planner/page.tsx:443` |\n| M4 | 🟢 Minor | 2-digit year always prepends \"20\" | `lib/parser.ts:855` |\n| M5 | 🟢 Minor | No type validation on prereq import array | `app/api/prereqs/import/route.ts:93` |\n| M6 | 🟢 Minor | `SELECT FROM` continuation regex too permissive | `lib/parser.ts:511` |\n| M7 | 🟢 Minor | Settings page missing | — |\n| M8 | 🟢 Minor | Solver targets cumulative only, not major GPA | `app/gpa/page.tsx` |\n\n---\n\n## Verdict: PASS WITH FIXES\n\nFix C1 and C2 before you use this for real. C1 will corrupt semester statuses on any future audit upload. C2 means the entire elective planning workflow (which is a stated core feature) is broken until you run the prereq scraper. The rest is real-app polish.","stop_reason":"end_turn","session_id":"7035cae2-a10c-47a3-941f-545681f0e516","total_cost_usd":1.4682407499999999,"usage":{"input_tokens":18,"cache_creation_input_tokens":113395,"cache_read_input_tokens":720064,"output_tokens":15976,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":113395,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"claude-sonnet-4-6":{"inputTokens":18,"outputTokens":15976,"cacheReadInputTokens":720064,"cacheCreationInputTokens":113395,"webSearchRequests":0,"costUSD":1.4682407499999999,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"fast_mode_state":"off","uuid":"7631ce05-991d-4379-84cc-33d85317634b"}
